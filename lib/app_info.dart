/// 앱 정보 화면에 표시하는 이름과 버전.
///
/// 이름은 고유명사라 번역하지 않고, 소개 문구는 언어별로
/// [AppLanguage.appDescription] 에 둔다. 버전을 올릴 때는 여기만 고치면 된다.
class AppInfo {
  AppInfo._();

  static const name = 'WorkSafe';
  static const version = 'v0.1';
}
