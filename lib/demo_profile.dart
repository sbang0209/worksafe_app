/// 홈 화면 상단 프로필에 표시할 시연용 사용자 정보.
///
/// 아직 계정별 사용자 데이터가 없어서, 로그인한 계정과 무관하게 이 고정값을 쓴다.
/// 표시 내용을 바꿀 일이 있으면 이 파일만 고치면 된다.
class DemoProfile {
  DemoProfile._();

  static const name = 'Alex';
  static const employeeNumber = '12345';
}
